// Test 1: Mixed regular and pygame blocks
game "Breakpoint Test" {
    width: 800
    height: 600
    background: black
}

player {
    color: cyan
    size: 30
    x: 100
    y: 100
    controls: arrows
    speed: 5
}

// Test pygame block with square brackets
debug[
    # This is raw pygame code
    font = pygame.font.Font(None, 36)
    text = font.render("Pygame Block Works!", True, (255, 255, 0))
    screen.blit(text, (10, 10))
]

ui {
    "Test UI" at top_center
}

